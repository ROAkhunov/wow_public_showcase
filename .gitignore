# Реальный срез данных с прода — НЕ коммитим.
# Внутри лежат наименования рекламодателей-физлиц (ФИО), решение по перс. данным не принято.
prototype/data.js

.env
venv/
.venv/
__pycache__/
*.pyc
node_modules/
.DS_Store

# Синхронизация с claude.ai/design: скопированные скрипты скилла и собранная
# выгрузка. Восстанавливаются командой из .design-sync/NOTES.md.
.ds-sync/
ds-bundle/
.design-sync/.cache/

# черновики вариантов вёрстки, лежат локально и в репозиторий не едут
design-system/cta-*.html
